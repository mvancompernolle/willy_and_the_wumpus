#pragma once

#ifndef TEXT_ALIGNMENT_H
#define TEXT_ALIGNMENT_H

enum HOR_ALIGNMENT {
	LEFT_ALIGNED,
	RIGHT_ALIGNED,
	HOR_CENTERED
};

enum VERT_ALIGNMENT {
	BELOW,
	ABOVE,
	VERT_CENTERED
};

#endif // TEXT_ALIGNMENT_H